@inject ITodoState state

<h4>Total todos: @state.Count</h4>

<ul class="list-group mt-4">
  
    @foreach(var item in TodoItems)
    {
        <Item @key="item.Id" TodoItem="item" />
    }

</ul>

@code {

    [Parameter]
    public IReadOnlyList<TodoItem> TodoItems { get; set; } = [];

}
